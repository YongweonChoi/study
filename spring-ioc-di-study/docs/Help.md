#  스프링 수행시 아래의 오류 발생시 조치
오류: 기본 클래스 com.example.BackendApplication을(를) 찾거나 로드할 수 없습니다.
원인: java.lang.ClassNotFoundException: com.example.BackendApplication 오류 해결해줘
mvn clean compile 수행 후 Maven 동기화 수행 => 스프링 재시작